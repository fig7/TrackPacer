package com.fig7.trackpacer.ui.pace

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class PaceViewModel(application: Application): AndroidViewModel(application) {
    private val runDist = ""
}